# Copy to keystore.properties (in the project root) and fill it in.
# The file is in .gitignore — never commit it.
storeFile=/path/to/your/upload-keystore.jks
storePassword=CHANGE_ME
keyAlias=upload
keyPassword=CHANGE_ME
